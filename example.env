# Memento MCP Configuration Example
# Copy this file to .env and configure with your API keys

# ===================================
# STORAGE CONFIGURATION
# ===================================
MEMORY_STORAGE_TYPE=neo4j
NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-neo4j-password
NEO4J_DATABASE=neo4j
NEO4J_VECTOR_INDEX=entity_embeddings
NEO4J_VECTOR_DIMENSIONS=2048
NEO4J_SIMILARITY_FUNCTION=cosine

# ===================================
# EMBEDDING PROVIDER CONFIGURATION
# ===================================
# Choose your embedding provider
# Options: 'voyageai', 'openai', or leave blank for auto-detect
# Auto-detect priority: voyageai > openai > default (mock)
EMBEDDING_PROVIDER=voyageai

# -----------------------------------
# Voyage AI Configuration (Recommended)
# -----------------------------------
# Voyage AI offers state-of-the-art embeddings with excellent semantic search quality
# Sign up: https://www.voyageai.com/
VOYAGE_API_KEY=your-voyage-api-key

# Model options:
#   - voyage-3-large (1024 default, configurable to 2048) - Best quality, recommended
#   - voyage-3 (1024 default, configurable) - Good balance
#   - voyage-3-lite (512 default) - Faster, smaller
#   - voyage-finance-2 (1024) - Finance domain
#   - voyage-law-2 (1024) - Legal domain
#   - voyage-code-2 (1536) - Code domain
VOYAGE_EMBEDDING_MODEL=voyage-3-large

# Note: voyage-3 models support output_dimension parameter (256-2048)
# Set NEO4J_VECTOR_DIMENSIONS above to control output size
# Lower dimensions = faster search, less storage
# Higher dimensions = better semantic quality

# -----------------------------------
# OpenAI Configuration (Alternative)
# -----------------------------------
# OpenAI embeddings are also supported
OPENAI_API_KEY=your-openai-api-key

# Model options:
#   - text-embedding-3-small (1536 dimensions) - Cost-effective
#   - text-embedding-3-large (3072 dimensions) - Higher quality, more expensive
#   - text-embedding-ada-002 (1536 dimensions) - Legacy model
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# ===================================
# COHERE RERANKING (Optional)
# ===================================
# Cohere reranking improves search result quality by re-ordering results
# Sign up: https://cohere.com/
# Note: Trial keys have rate limits - production keys recommended for heavy use
COHERE_API_KEY=your-cohere-api-key

# Model options:
#   - rerank-english-v3.0 (default) - Best for English content
#   - rerank-multilingual-v3.0 - For multilingual content
COHERE_RERANK_MODEL=rerank-english-v3.0

# ===================================
# HYBRID SEARCH CONFIGURATION
# ===================================
# Enable hybrid search combining vector similarity and keyword (BM25) search
# Uses Reciprocal Rank Fusion (RRF) to combine results
ENABLE_HYBRID_SEARCH=true

# RRF constant (default: 60)
# Higher values = more balanced between vector and keyword results
# Lower values = more weight on top-ranked results
RRF_K=60

# ===================================
# DEBUG CONFIGURATION
# ===================================
# Enable debug logging
DEBUG=false

# Mock embeddings for testing (no API key required)
# Set to 'true' to use random vectors instead of real embeddings
MOCK_EMBEDDINGS=false